---
title: Experiment Log
updated: 2026-08-18
status: current
---

# Experiment log

Append-only run log — **rows are never deleted**; failed runs with takeaways are the point. The ladder *definitions* (E00–E15, commands, prerequisites) live in `experiments/README.md`; this file records what was actually run. Judge runs against the floors in [[baselines]].

Every row: date, ladder ID, short commit hash of the code that ran, config deltas, outcome, takeaway.

| Date | ID | Commit | Config | Outcome | Takeaway |
| --- | --- | --- | --- | --- | --- |
| 2026-08-13 | E00 | 2bc3126 | upstream `g1-29dof-fast-sac`, mjwarp, 512 envs, 300 it | ran on GPU, ~3.8 GB VRAM — but first attempt on PyPI mujoco-warp 3.11.0 diverged (NaN rewards, 1e12 ang. vel., 4-step episodes) | pin mujoco_warp git `ecaef88`; PyPI 3.11.0 breaks physics for both G1 and A3 |
| 2026-08-13 | E01 | 9987ce6 | `a3-ultra-fast-sac`, 512 envs, 200 it (`logs/everest-a3/20260813_233010`) | trains on GPU (WarpBackend + CUDA graph), 0 nefc overflows, ~30 s/it wall-clock | chain is correct but local is ~30 s/it → train in the cloud, validate locally |
| 2026-08-13 | — | 9987ce6 | stability suite, PD-stand policy, full 68 scenarios | 25/68 survived; max push 0.2–0.3 m/s | the documented floor every trained policy must beat (`results/stability/pd_stand_baseline.json`) |
| 2026-08-13 | E12 | e28cc96+ | `a3-ultra-getup` (PPO) mjwarp smoke, 64 envs, 3 it | env+pose-bank+obs(93/97)+rollout all wired; PPO update NaN'd ("std >= 0") | NaN is NOT the get-up task: control run below reproduces it with pure locomotion |
| 2026-08-13 | E07-pre | e28cc96+ | `a3-ultra-ppo` (plain upstream locomotion PPO) mjwarp, 64 envs, 3 it | NaN in first PPO update, zero get-up code involved | **PPO+MJWarp is broken locally even for locomotion**; FastSAC survives → MJWarp smoke = FastSAC only, all PPO runs = IsaacSim cloud |
| 2026-08-13 | E12 | e28cc96+ | `a3-ultra-getup-fast-sac` mjwarp smoke, 64 envs, 5 it, njmax_per_env 768, real fallen starts + assist force | **5/5 iterations, checkpoint saved, clean exit** | get-up pipeline works end-to-end locally; lying contacts need njmax_per_env ≥1024 (one 857 frame); ready for Lambda (`scripts/cloud/train_a3_getup_cloud.sh`) |
| 2026-08-14 | E02/E06 | b5622c2 | `a3-ultra-fast-sac` on Lambda, IsaacSim, 4096 envs, **50 000 it** (204.8 M samples, ~21 k FPS), ckpts every 5 k (`checkpoints/cloud_20260814_012617-…`) | episode length 28.7 → 1001 steps (never terminates early); `rew_tracking_lin_vel` 0.015 → **1.272** (clears its own 0.95 nightly gate), `rew_tracking_ang_vel` 0.008 → **0.559** (below its 0.8 gate); penalty curriculum fully ramped to 1.0 | first real locomotion policy. Linear-velocity tracking is the strength, **angular is the weak axis** — no heading term in the obs, so yaw error integrates. Next run: heading obs term or heavier `tracking_ang_vel` weight |
| 2026-08-14 | E08c | 2e780f0+ | arm-obs treatment under worst-case skills: 3 Hz swings, 0.5 s slams between joint extremes, full-range poses resampled every 0.4 s, 3/6 kg per hand, rough+1.5 m/s pushes — raw vs zero-mask vs clamp | **raw 5/20, zero-mask 20/20, clamp 20/20**. Policy's own arm deviation in normal walking is only **±0.018 rad**, so clamp ≈ zero. Always-on masking is free when idle (velerr 0.173→0.175). Waist responds the same way (1 Hz twist: falls 8.1 s raw, OK masked) | **no retrain needed for stability.** Masking preserves stability but not tracking under fast arm motion: 3 Hz antiphase swing → ang-vel error 2.24 rad/s vs 0.066 baseline |
| 2026-08-14 | E08b | 040fe82+ | arm-skill override: 7 scripted upper-body motions × 4 contexts, arm targets replacing the policy's (`scripts/eval/sim2sim_arms.py`) | **17/28 survive**; masking the arm `dof_pos`/`dof_vel` obs channels → **28/28** (worst tilt 75.1° → 6.3°) | the arms are policy-owned (pose weight 50), so a skill *overrides* 14 of 29 outputs. Failure is **off-distribution observation, not balance** — static CoM shift is ≤2.75 cm against a 14.7 cm margin. Interim contract: mask arm obs channels when a skill owns the arms. Real fix: retrain with randomized upper-body poses |
| 2026-08-14 | E08 | 040fe82 | sim2sim gate: `model_0050000.onnx` in MuJoCo classic 3.11.0, 50 Hz, generated MJCF, seeded (`scripts/eval/sim2sim_suite.py`) | **68/68 stability grid** vs PD-stand control **26/68** on the same harness (recorded floor 25/68); max recoverable push **2.0–3.0 m/s standing / 2.5–4.0 m/s walking** vs 0.2–0.3 floor; extended showcase **37/41**; torque saturation ≤0.3%; quiet-stand jitter 0.001 | policy transfers across the PhysX→MuJoCo boundary untuned — M1 has a passing gate. Failures are only *combined* alpine conditions (rough+slope+low µ+gusts) and µ0.1; each ingredient alone is survivable. `docs/onnx_policy_interface.md` had the obs order wrong — actual order is alphabetical by term name. Detail: `docs/sim2sim_locomotion_report.md` |
| 2026-08-14 | E13 | b5622c2 | `a3-ultra-getup` (single policy, ALL postures) IsaacSim cloud, 4096 envs, ~17.4k it incl. resume w/ up_threshold 0.45 | **plateaued: success 0.30 ≈ supine bank share, assist never annealed** (episodes healthy 501 steps, KL/losses clean; quality rewards kept improving, capability did not) | one policy can't cover mixed postures — HoST's own limitations section says posture mixing hurts; split into rollover + get-up policies (decisions 2026-08-15) |
| 2026-08-15 | E09-pre | 3023448+ | v2 extension bring-up: MJWarp smoke of **all six stages** (s0–s4, s4-lcp), 64 envs, 14–25 it each, `compile False` | all six construct, train and export. Obs dims: **s0 100** (= v1 exactly), s1 575, s2 645, s3/s4 692 (+117 scandots). `vel_est_rms_ms` falls 1.17→0.66 in 25 it; heading command, CAM, arm curriculum, CNN encoder and the LCP double-backward all run clean. s1 died at it 35 on the known MJWarp `nefc overflow`→NaN, not on our code | pipeline is wired. Three reward terms were badly scaled and were caught **here, not on Lambda**: `penalty_feet_impact` raw 9.7e4/episode, `penalty_dof_acc` raw 3.1e11, CAM damping unbounded — all now normalised and clamped (see [[decisions]]). **Always scale-check a new reward term on a 25-iteration smoke before buying cloud hours.** |
| 2026-08-15 | E08 | 3023448+ | v1 `model_0050000.onnx` re-graded through the **layout-driven** sim2sim harness (`ObsLayout`, per-term history, scandots, heading regulation) | **68/68 stability grid**, 0.431 m/s @ cmd 0.5, 0.842 @ 1.0, jitter 0.0346/0.0427, 2.0 m/s push survived — identical to the recorded E08 numbers | the harness refactor is behaviour-neutral for v1, so v2 stages can be graded on the same scale. v1 policies take the `ObsLayout.v1_default` fallback; v2 policies carry their own layout in ONNX metadata |
| 2026-08-16 | E13b | b39eddb | `a3-ultra-getup` v3 (supine-only split, KSI waypoints, authority curriculum) IsaacSim cloud, 4096 envs, ~13.9k it / 1.37B samples | **wedged, not slow: success 0.0000, `assist_scale` 1.0 and `action_authority` 2.0 never moved a single step.** `rose_rate` 0.394 sat inside the curriculum's [0.25, 0.6] dead band; ~0.30 of it was free credit from standing anchors (`easy_start_prob` = 0.05 + 0.25·assist_scale). Net rise rate from fallen starts ≈ 13%, *with* 350 N of assist | the posture split did not fix it because the curriculum could not move. Root cause below — abort rules now in the launcher |
| 2026-08-16 | E13c | b39eddb+ | root-cause probe, no training: `scripts/diagnostics/check_getup_terminal.py` — standing spawn, real policy, sweep β × exploration noise, score `_task_success_condition` | **at β (action authority) 2.0 the terminal gate is unsatisfiable with ZERO noise** — 1/100 steps held, gate passes 0.2%, mean `max\|dof_vel\|` over legs+waist **1.36 rad/s** vs the 1.0 threshold. At β 1.0: 596/100, jvel 0.016. Also: the strictly-consecutive 100-step conjunction fails even at β 1.0 under converged noise (57/100, gate passes 95.5%) | **β 2.0 alone pinned `getup_success_rate` at 0, and β only annealed when the success-linked rose proxy cleared its threshold — a deadlock with no exit.** Ruled out first: actuator torque (knee 320 Nm, τ/(m·h) 3.1 vs G1's proven 3.0), the assist force (a standing robot holds *better* with it), and gate reachability (the v1 locomotion policy clears the handoff gate 100% of steps with 50× margin) |
| 2026-08-16 | E13-eval2 | b39eddb+ | v1 get-up `model_27999.onnx` re-graded through the **corrected action map** (wrist soft-freeze applied, applied-action observation feedback), 12 episodes × 4 postures | **0/12, peak pelvis 0.24–0.32 m** (previously recorded 0.55–0.90 m) | the earlier E13-eval number was measured with the wrist freeze missing, which let the policy push off with wrists it never had. Corrected, v1 barely sits up at all. Verdict unchanged, mechanism worse |
| 2026-08-15 | E13-eval | 0485a77+ | v1 get-up `model_27999.onnx` (28k plateau) rolled out **unassisted** in MuJoCo from the real fallen-pose bank — 3 episodes × supine/prone/side_left/side_right, plus 6 sit/kneel/crouch waypoints and 1 standing start (`scripts/eval/getup_video.py`) | **0/12 fallen, 0/6 waypoint, 0/1 standing.** Torso reliably rotates upright (tilt → 13–20°) and the pelvis reaches **0.55–0.90 m**, then stalls in a sit/kneel and settles back to 0.27–0.46 m. From a *standing* start it actively falls (1.07 → 0.22 m) | the E13 "success 0.30" was scored **with the assist force still at full scale** (that row: "assist never annealed"), so it measured assist-supported rising, not the policy's. Unassisted, v1 has no stand: it learned sit-up, not get-up — which is exactly the torque-critical phase the v3 assist re-gate targets. **Grade every get-up checkpoint unassisted before trusting its success curve.** Video: `results/videos/getup_v1_28k/` |
| 2026-08-16 | E09 | b39eddb | v2 ladder on Lambda A100, IsaacSim, 4096 envs, seed 1, buffer 256 — **s0** 10k, **s1** 50k, **s2** 80k, **s3** 100k, **s4** 30k, each trained from scratch (~14 GPU-h, `checkpoints/cloud_2026081[56]_*`) | **s0 and s1 pass; s2, s3, s4 are unusable.** s0 68/68 grid (lin-vel err 0.183, ang 0.182), s1 **68/68** grid vs PD-stand 26/68, best ckpt `model_0040000` at lin-vel err **0.154**, ang **0.150** (0.406 → 0.150 over training), jitter 0.032, showcase 36/41, walking push recovery to 4.0 m/s. `vel_est_rms_ms` converged to **0.038 m/s**. s2/s3/s4 scored **0/41 each**, falling in 0.3–2.2 s on every scenario including quiet stand, action jitter **1.3–1.65** vs s1's 0.032 | two independent bugs, both in the arm-ownership handling (see [[decisions]]). (1) The arm channels were left **undefined**: overridden on 80% of envs so no gradient, and `arms_free` zeroed the arm pose weight on the rest — open-loop on a standing observation the arms command **|a| up to 11.5 (2.9 rad)** while the legs stay at 0.45. (2) `penalty_action_rate` differenced the *commanded* arm sinusoid, raw 132 → 571 → 720 at weight −2.0, driving net per-step reward to **−0.81 (s2) / −2.75 (s4)** against a +0.50 alive bonus — with no death penalty, falling early became optimal, and episode length duly fell 1000 → 801 → 797 → 754. **Both were invisible in TensorBoard because every individual reward curve looked healthy; nobody plotted their sum.** `Env/net_reward_per_step` now exists for exactly this. Confirmed good: `Env/cam_z` measured **0.0120–0.0136** against the assumed `CAM_REF_AMPLITUDE` 0.01, so that constant needs no retune |
| 2026-08-16 | E09-fix | (this commit) | ownership-masked `pose` / `penalty_action_rate` / `penalty_action_jerk` / `penalty_dof_acc` / `penalty_torques`, new `penalty_arm_off_target` (w −0.1, scale-checked against the measured S1 per-step budget), arm curriculum gate 750 → 950/800, scandots de-double-noised, single `GradScaler.update()`, `_cam_active` cache hardened, heading mode cleared in eval, `StandPolicy.layout` restored. Ladder rebuilt so **S1..S4 share one 692/707 observation contract** and S2+ resume by `--training.checkpoint` with cumulative iterations (50k → 130k → 230k → 260k) | 11 new tests in `tests/test_v2_reward_masking.py` prove the masked `pose` and `penalty_action_rate` are **bit-identical to holosoma's own functions** when no arm command is registered, so S0/S1 stay neutral. MJWarp smoke: s1 (692/707, CNN encoder) trains; **s1→s2 resumed at step 20 and ran to 40; s3→s4 resumed at 20 and ran to 32**. s2→s3 NaN'd locally, but s3 *from scratch* NaN'd earlier (iteration 6, `nefc overflow`), so that is the documented MJWarp-on-slopes limit, not the resume | the ladder is now a curriculum rather than four unrelated runs — which is also what makes S4's small budget defensible, since it continues S3 instead of starting cold on the hardest config. **Costs one S1 retrain** (~3.3 h): the existing 505-dim S1 checkpoint cannot seed the chain. Re-run is S1 → S2 → S3 → S4, ~15 GPU-h |
| 2026-08-18 | E09b | 7c1cc1f | v2 ladder **re-run with the ownership fixes**, A100-SXM4-40GB, IsaacSim, 4096 envs, seed 1, buffer 256, **chained by `--training.checkpoint`** — s1 0→50k, s2 →130k, s3 →230k, s4 →260k, ~19 h wall clock (`checkpoints/cloud_20260817_*`) | **Both failure modes gone.** `Env/net_reward_per_step` **+0.164 / +0.236 / +0.257 / +0.245** (was −0.81 / −2.75) and episode length **1000 / 999 / 998 / 999** (was 801/797/754). Raw `penalty_action_rate` **131.7 → 66.3 → 39.1 → 38.7** (was 132 → 571 → 720) — masking the commanded arms made the *legs* smoother too. `Env/arm_amplitude` reached 1.0 at ~70k with episodes at the cap, so the 950 gate did not over-tighten. **All four stages 68/68 on the stability grid** (was 0/41 for s2–s4). Showcase s1 **38/41**, s2 36/41, s3 35/41, s4 **38/41** vs v1's 37/41. Sweep best: s1 `model_0045000` velerr **0.148** angerr **0.144** jitter **0.032**; s4 `model_0234000` velerr 0.226 angerr **0.127** jitter 0.082. `rew_tracking_ang_vel` 0.538 / 0.785 / **0.821** / **0.843** — s3 and s4 are the first policies to clear the 0.8 gate. `vel_est_rms_ms` 0.038–0.041; `Env/cam_z` 0.0090–0.0095 against the assumed 0.01 | the two ownership bugs were the whole story ([[decisions]]). **S1 promoted** — better than v1 on every axis and the first improvement on the M1 floors. S2/S3/S4 each failed their own gate: arm suite 13/28 (needs 26), alpine combos 0/3 plus new `friction_mu0.2` and `push_front_2` regressions, jitter **up** 0.069→0.082 where S4 promised −30%. Chaining is proven end-to-end: `global_step` 260000 at s4, and `arm_amplitude` visibly resumes at 50k/130k/230k |
| 2026-08-18 | E09c | 7c1cc1f | arm-observation **3x4 ablation** (`sim2sim_arms.py --arm-obs {full,none,state,target}`), v1 vs S1 vs S4, 28 skill×context scenarios each. `state` hides the arm `dof_pos`/`dof_vel`; `target` hides `upper_body_target` | v1 **17 / 28 / 28 / 17**, S1 **14 / 28 / 19 / 8**, S4 **13 / 16 / 10 / 6**. Harness validated by construction: v1 has no `upper_body_target`, so `state`≡`none` and `target`≡`full` — and the numbers match exactly | **Feeding the arms in is not the fix; component C made robustness worse.** S4 — the only policy trained with arms driven *and* told their target one step early — is the worst in **every** mode and never clears 16/28, while v1 with no arm channel at all hits 28/28. The harness does feed the skill's real target, so this is not information starvation: `UpperBodyCommand` (0.2–2.0 Hz, held poses) is far narrower than the suite (3 Hz swings, 0.5 s slams), and a narrow learned compensation is worse than none. **S1 keeps v1's 28/28 masked contract**; S4 does not (16/28). If masking, mask **both** channels — for S1 hiding the target alone (8/28) is worse than hiding nothing (14/28). Decision: arm skills become a physics disturbance, never an action override |
