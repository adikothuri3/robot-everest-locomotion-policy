---
title: Experiment Log
updated: 2026-08-14
status: current
---

# Experiment log

Append-only run log — **rows are never deleted**; failed runs with takeaways are the point. The ladder *definitions* (E00–E15, commands, prerequisites) live in `experiments/README.md`; this file records what was actually run. Judge runs against the floors in [[baselines]].

Every row: date, ladder ID, short commit hash of the code that ran, config deltas, outcome, takeaway.

| Date | ID | Commit | Config | Outcome | Takeaway |
| --- | --- | --- | --- | --- | --- |
| 2026-08-13 | E00 | 2bc3126 | upstream `g1-29dof-fast-sac`, mjwarp, 512 envs, 300 it | ran on GPU, ~3.8 GB VRAM — but first attempt on PyPI mujoco-warp 3.11.0 diverged (NaN rewards, 1e12 ang. vel., 4-step episodes) | pin mujoco_warp git `ecaef88`; PyPI 3.11.0 breaks physics for both G1 and A3 |
| 2026-08-13 | E01 | 9987ce6 | `a3-ultra-fast-sac`, 512 envs, 200 it (`logs/everest-a3/20260813_233010`) | trains on GPU (WarpBackend + CUDA graph), 0 nefc overflows, ~30 s/it wall-clock | chain is correct but local is ~30 s/it → train in the cloud, validate locally |
| 2026-08-13 | — | 9987ce6 | stability suite, PD-stand policy, full 68 scenarios | 25/68 survived; max push 0.2–0.3 m/s | the documented floor every trained policy must beat (`results/stability/pd_stand_baseline.json`) |
| 2026-08-13 | E12 | e28cc96+ | `a3-ultra-getup` (PPO) mjwarp smoke, 64 envs, 3 it | env+pose-bank+obs(93/97)+rollout all wired; PPO update NaN'd ("std >= 0") | NaN is NOT the get-up task: control run below reproduces it with pure locomotion |
| 2026-08-13 | E07-pre | e28cc96+ | `a3-ultra-ppo` (plain upstream locomotion PPO) mjwarp, 64 envs, 3 it | NaN in first PPO update, zero get-up code involved | **PPO+MJWarp is broken locally even for locomotion**; FastSAC survives → MJWarp smoke = FastSAC only, all PPO runs = IsaacSim cloud |
| 2026-08-13 | E12 | e28cc96+ | `a3-ultra-getup-fast-sac` mjwarp smoke, 64 envs, 5 it, njmax_per_env 768, real fallen starts + assist force | **5/5 iterations, checkpoint saved, clean exit** | get-up pipeline works end-to-end locally; lying contacts need njmax_per_env ≥1024 (one 857 frame); ready for Lambda (`scripts/cloud/train_a3_getup_cloud.sh`) |
| 2026-08-14 | E02/E06 | b5622c2 | `a3-ultra-fast-sac` on Lambda, IsaacSim, 4096 envs, **50 000 it** (204.8 M samples, ~21 k FPS), ckpts every 5 k (`checkpoints/cloud_20260814_012617-…`) | episode length 28.7 → 1001 steps (never terminates early); `rew_tracking_lin_vel` 0.015 → **1.272** (clears its own 0.95 nightly gate), `rew_tracking_ang_vel` 0.008 → **0.559** (below its 0.8 gate); penalty curriculum fully ramped to 1.0 | first real locomotion policy. Linear-velocity tracking is the strength, **angular is the weak axis** — no heading term in the obs, so yaw error integrates. Next run: heading obs term or heavier `tracking_ang_vel` weight |
| 2026-08-14 | E08c | 2e780f0+ | arm-obs treatment under worst-case skills: 3 Hz swings, 0.5 s slams between joint extremes, full-range poses resampled every 0.4 s, 3/6 kg per hand, rough+1.5 m/s pushes — raw vs zero-mask vs clamp | **raw 5/20, zero-mask 20/20, clamp 20/20**. Policy's own arm deviation in normal walking is only **±0.018 rad**, so clamp ≈ zero. Always-on masking is free when idle (velerr 0.173→0.175). Waist responds the same way (1 Hz twist: falls 8.1 s raw, OK masked) | **no retrain needed for stability.** Masking preserves stability but not tracking under fast arm motion: 3 Hz antiphase swing → ang-vel error 2.24 rad/s vs 0.066 baseline |
| 2026-08-14 | E08b | 040fe82+ | arm-skill override: 7 scripted upper-body motions × 4 contexts, arm targets replacing the policy's (`scripts/eval/sim2sim_arms.py`) | **17/28 survive**; masking the arm `dof_pos`/`dof_vel` obs channels → **28/28** (worst tilt 75.1° → 6.3°) | the arms are policy-owned (pose weight 50), so a skill *overrides* 14 of 29 outputs. Failure is **off-distribution observation, not balance** — static CoM shift is ≤2.75 cm against a 14.7 cm margin. Interim contract: mask arm obs channels when a skill owns the arms. Real fix: retrain with randomized upper-body poses |
| 2026-08-14 | E08 | 040fe82 | sim2sim gate: `model_0050000.onnx` in MuJoCo classic 3.11.0, 50 Hz, generated MJCF, seeded (`scripts/eval/sim2sim_suite.py`) | **68/68 stability grid** vs PD-stand control **26/68** on the same harness (recorded floor 25/68); max recoverable push **2.0–3.0 m/s standing / 2.5–4.0 m/s walking** vs 0.2–0.3 floor; extended showcase **37/41**; torque saturation ≤0.3%; quiet-stand jitter 0.001 | policy transfers across the PhysX→MuJoCo boundary untuned — M1 has a passing gate. Failures are only *combined* alpine conditions (rough+slope+low µ+gusts) and µ0.1; each ingredient alone is survivable. `docs/onnx_policy_interface.md` had the obs order wrong — actual order is alphabetical by term name. Detail: `docs/sim2sim_locomotion_report.md` |
| 2026-08-14 | E13 | b5622c2 | `a3-ultra-getup` (single policy, ALL postures) IsaacSim cloud, 4096 envs, ~17.4k it incl. resume w/ up_threshold 0.45 | **plateaued: success 0.30 ≈ supine bank share, assist never annealed** (episodes healthy 501 steps, KL/losses clean; quality rewards kept improving, capability did not) | one policy can't cover mixed postures — HoST's own limitations section says posture mixing hurts; split into rollover + get-up policies (decisions 2026-08-15) |
